#!/bin/bash
outdir="out"
tmc_path="../Daggerc/solidity/Tmc3.sol"
TC_path="../Daggerc/examples/translateCurrencySwap.bahr"
con_exe_path="test/contract_execution/translateCurrencySwapTmc3.js"

rm -rf $outdir
./compile_and_deploy.pl $outdir $tmc_path 1000 "'DKK'" 0 "'DKK'"
./compile_and_deploy.pl $outdir $tmc_path 1000 "'USD'" 0 "'USD'"
./compile_and_deploy.pl $outdir $TC_path
./run_code $outdir $con_exe_path
# sleep 240
# ./run_code $outdir $con_exe_path
# ./run_code $outdir $con_exe_path
